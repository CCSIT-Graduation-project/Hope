import 'package:flutter/material.dart';

class ManageSymptomsTracker1 extends StatelessWidget {
  final String title;
  const ManageSymptomsTracker1({Key? key, required this.title})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
